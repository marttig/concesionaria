echo "# concesionaria" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/marttig/concesionaria.git
git push -u origin main

git config user.name "..."
git config user.email "..."